[pyprland]

plugins = [
  "scratchpads",
  "magnify",
]

[scratchpads.term]
animation = "fromTop"
# Let Hyprland finish the slide-out before moving the window to its hidden workspace.
hide_delay = 0.4
command = "kitty --class kitty-dropterm"
class = "kitty-dropterm"
size = "75% 60%"
